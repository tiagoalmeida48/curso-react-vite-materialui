# Como utilizar o json-server

1. Abrir o terminal
2. Executar o comando:
    ```
    yarn run json-server -w -p 3333 ./database.json
    ```
    ou
    
    ```
    npm run json-server -w -p 3333 ./database.json
    ```